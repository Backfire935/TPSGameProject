#pragma once

namespace Announcement
{
	const FString NewMatchStartsIn(TEXT("开始新的匹配:"));

	const FString ThereIsNoWinner(TEXT("没有MVP玩家产生"));

	const FString YouAreTheWinner(TEXT("你是本场比赛的MVP"));

	const FString ManyWinner(TEXT("本场比赛产生了多名优胜者:\n"));

	const FString TeamsTiedForTheWin(TEXT("两支队伍打成平手."));

	const FString RedTeam(TEXT("啊对对队"));

	const FString BlueTeam(TEXT("你说的都队"));

	const FString RedTeamWins(TEXT("啊对对队赢得胜利."));

	const FString BlueTeamWins(TEXT("你说的都队赢得胜利."));

}
